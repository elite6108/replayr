{
  "name": "replay-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --ip 127.0.0.1 --port 8787",
    "sync-env": "node scripts/sync-dev-vars.mjs",
    "setup-stripe": "node scripts/setup-stripe.mjs",
    "wire-stripe": "node scripts/wire-stripe-local.mjs",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "bunny-spike": "node scripts/bunny-capability-spike.mjs"
  },
  "dependencies": {
    "aws4fetch": "^1.0.20"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4",
    "wrangler": "^4.28.0"
  }
}
